.. highlight:: shell

============
Installation
============


Stable release
--------------

To install log_aggregator_server, run this command in your terminal:

.. code-block:: console

    $ pip install log_aggregator_server

This is the preferred method to install log_aggregator_server, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for log_aggregator_server can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/GTedHa/log_aggregator_server

Or download the `tarball`_:

.. code-block:: console

    $ curl  -OL https://github.com/GTedHa/log_aggregator_server/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/GTedHa/log_aggregator_server
.. _tarball: https://github.com/GTedHa/log_aggregator_server/tarball/master
